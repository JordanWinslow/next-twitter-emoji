name: CI

on: [push, pull_request]

env:
  DATABASE_URL: "https://any.com"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: npm install

      - name: Typecheck
        run: npm run typecheck

      - name: ESLint
        run: npm run lint
